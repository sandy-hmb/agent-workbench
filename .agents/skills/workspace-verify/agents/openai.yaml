interface:
  display_name: "Workspace Verify"
  short_description: "Run validation and record work item evidence"
  default_prompt: "Use $workspace-verify to run the current work item's authorized validation and record progress for handoff."
