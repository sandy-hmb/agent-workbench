# {title} 实施计划

依据：[需求](../requirements/requirements.md)、[设计](../design/design.md)。每项任务先引用主设计 D 编号；只有需要实现细节时才追加附件章节链接。

## 执行概览

记录 feature slug、目标仓、基线、工作分支、执行方式、共同必读和全局约束。

## 任务

只有存在至少两个自然业务或交付领域时才增加三级分组；分组不带复选框，不计入进度。

### 1. 示例分组

- [ ] T01 交付任务

  用一至两句话说明完成后的可观察结果。

  依据：R1、[D01](../design/design.md#d01)；细节（如适用）：[D01 扩展](../design/data-model.md#d01-字段设计)
  依赖：无

  **改动位置**

  - `path/to/file`：本任务承担的职责

  **实施步骤**

  1. 为非平凡行为先写最小失败测试。
  2. 完成最小实现和必要回归。

  **验证**

  工作目录：`repository`

  ```bash
  command \
    --long-option value \
    test
  ```

  通过条件：目标检查实际执行并通过。

## 整体验证与完成条件

记录跨任务回归、需求覆盖、范围核对和交付边界；不要复制每个任务已经定义的验证。
