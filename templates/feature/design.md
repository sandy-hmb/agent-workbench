# {title} 技术设计

依据：[需求](../requirements/requirements.md)。审阅状态见 [README](../README.md)。本文件始终是完整、自包含的技术设计和唯一主入口；附件只扩展本文件的 D 决策。

**目标：** 用一句话说明本设计实现的结果。

**选定方案：** 用两至三句话概括技术方案；不存在实质取舍时直接说明选择。

**关键约束：** 只列适用于整个方案且会影响实现的约束。

## 本轮变更

首次开发说明初始方案；后续迭代简述技术变化。正文始终维护完整的当前目标设计，不写当次会话的执行授权或阶段进度。

## 现状与方案选择

说明现状、复用与新增边界、选定方案和总体组件关系。存在真实技术取舍时比较二至三个可行方案并说明推荐理由；没有取舍时不制造备选方案。

## 组件与关键流程

按关键业务流程说明组件、数据流、接口、数据模型、状态与错误；D 决策就近记录，不在概览和决策中完整重述同一流程。跨流程共有的数据/API 概览才单列；读完主设计仍须能理解全部关键结论。复杂字段或契约按需链接附件。

<a id="d01"></a>

## D01 关键决策

只为需要稳定引用的关键决策编号，并放在对应流程附近，说明选择、理由、影响和风险。按内容选择表达方式：

- 角色权限、字段映射或方案比较使用表格。
- 审批、事务、迁移或发布顺序使用编号步骤。
- 简单选择保留短段落；具体风险在本决策附近说明。

## 跨模块风险、兼容、回退与验证

只汇总跨多个决策的风险、回退边界和整体验证，不重复每个 D 决策已说明的风险。

## 需求覆盖

在全部 D 决策之后列出 R 编号到 D 编号的映射。

## 可选附件

附件必须声明扩展的 D 编号并链接回本文件。仅在已确认需要时创建；不在这里搬走 D 决策或必要摘要。

```markdown
- [数据模型详细设计](data-model.md#d01-字段设计)：展开 D01 的字段、DDL、迁移与回退。
- [接口集成详细设计](api-integration.md#d02-接口契约)：展开 D02 的请求响应、鉴权、联调与发布依赖。

流程、调用、状态、实体关系和模块依赖优先用 Mermaid；字段约束、接口样例和决策理由继续使用表格或文字。API 或数据模型只有在需要独立审阅、对接或执行时才拆附件。
```
