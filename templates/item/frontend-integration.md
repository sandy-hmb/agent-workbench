# {title} 前端联调指南

适用端：{client}｜对应版本：{version}｜接口文档：{api_reference}｜文档状态：设计稿

## 页面改动

| 页面或场景 | 前端需要调整的行为 | 接口 |
|---|---|---|

## 接口说明

按场景列出接口、必要请求和响应示例、字段语义、页面处理及特殊错误。示例注明完整响应或局部片段；完整字段定义引用接口文档。保留本次容易接错的单位、ID、空值、显式零和成功判断规则，不重复已有公共约定。

## 联调注意事项

只写兼容切换、必要测试数据及影响接入的限制。部署可用性单独说明，未确认时写“未确认”，不从文档状态推断环境可用。

<!-- 生成位置：artifacts/frontend-integration.md。删除模板说明与无关内容；实现后在同一文件核对字段、序列化、包装、错误及页面行为，更新版本并改为“已按实现核对”，从 README 登记入口。 -->
