# 变更记录

## 未发布

- 工作区布局收敛为 `config/`、`repositories/`、`items/`、`extensions/` 与 `runs/`：根目录只保留规则和事实；共享配置、本机配置、Workflow 草稿与生效配置统一进入 `config/`。
- 工作区配置主版本升级为 4；旧 v3 布局不再读取，需要在新目录初始化或按当前布局重新建立本地状态。

- 修复交付物变化后仍可沿用旧验证完成的问题；显式重审正确失效当前任务，新增草稿依赖不再导致重审失败。
- 已结束工作项新增或重开未完成验收时提示开启下一轮，保留历史完成结论。
- 普通工作项与小改可按仓库、路径发现规范，返回作用范围、版本、必要入口诊断及事实来源。
- 精简任务接手包，R/D 只提供定位，验证记录返回简短下一步；增加四类读取成本场景，区分首次读取与同会话复用。
- 工作台修复大列表分页、当前详情刷新与旧请求迟到覆盖；接手包补齐阻塞和待验收，交付文档可直接定位。

## 2.0.0 - 2026-09-25

- 统一使用 item 工作项命名；文档只维护内容，审批、任务完成、阻塞和交付由 Kit 统一记录，README 与验证摘要自动生成。
- 普通需求默认整体验证；独立任务编号持续递增，旧证据不能完成新一轮任务。
- 新增受控调整、开发阻塞与取消；取消后可开启新一轮，历史事实保持可追溯。
- 任务验证只读取目标仓；无变化更新和重复摘要不修改文件或状态版本。
- 摘要优先显示未解决事项，超限时给出剩余数量和完整查询入口。
- 代码按职责分包，统一 CLI 输入诊断与测试夹具；取消全局活动需求指针和重复验证记录命令。
- IntelliJ 工作台同步使用 Inspect 2，按角色定位文档，展示阻塞和取消状态，完成操作由 Kit 再次核对验收条件。

### 破坏性变更与升级动作

- 仅支持新版配置和 item 工作项；旧 feature 命令、目录和证据格式不再读取，不提供迁移。
- 旧本地数据保持原状。请在新目录初始化，并同步安装配套工作台插件 1.0.0。

## 1.7.1 - 2026-09-23

- 修复公共 Kit 快进更新后的健康检查：更新后使用新版本脚本独立执行检查，避免同一进程混用更新前后的模块导致已拉取但命令报错。

## 1.7.0 - 2026-09-23

- 新工作项按活动和风险选择审阅、文档与验证强度；普通活动使用 `change.md`，复杂需求按需维护 `requirements.md`、`design.md` 和 `plan.md`，旧 WorkItem 仍可按原流程续接。
- Inspect 新增工作项定向投影，减少无关仓库与证据读取；验证收尾支持真实来源和结果适用性，不从本地通过推断部署或外部验收。
- 维护模式支持同父目录下的独立兄弟仓任务与证据归属，跨仓进度、快照和分支比较按实际目标仓计算。
- 明确 WorkItem 文档职责和按需生成时机，新增三部分前端联调指南模板；设计稿与实现核对版维护同一文件，README 记录实际交付状态。
- 验证批次支持可选的 `verificationScope`、`pendingExternalChecks`，摘要保留待外部验证事项、批次问题和交付引用；旧证据缺失信息显示“未记录”。
- `brief --check` 检查已有文档的 README 导航及交付说明链接；导航遗漏仅警告，支持通过目录 README 索引成组交付物。
- IDEA 插件 `0.5.0` 需与本版本 Kit 配套使用；旧需求数据兼容，新旧程序版本混用不作承诺。

## 1.6.3 - 2026-09-13

- 修复 v2 验证记录的 Inspect 投影，使批次、检查和结果字段继续满足公开 `1.1` 契约及 IDE 插件消费要求。

## 1.6.2 - 2026-09-13

- 体检不再把结构化证据归档和事务备份当作当前文档检查，避免可恢复备份产生错误的断链诊断。

## 1.6.1 - 2026-09-13

- 修复旧验证批次时间字段带行尾空白时迁移失败，以及同一证据多次引用相同命令时被误判为写入冲突的问题。

## 1.6.0 - 2026-09-13

- 新需求默认把任务证据和验证批次保存为可校验、可分页查询的结构化记录；人类验证摘要保持固定规模。
- 状态与续接命令默认省略逐任务证据正文，需要追溯时可按任务、批次或 cursor 显式查询。
- 旧验证记录继续只读兼容，可先预览再显式迁移或压缩；操作前保留带哈希备份，失败或中断可恢复且不删除证据正文。
- Workflow Action 流水保留在本地 Run 中，验证摘要只展示最新状态，不再持续追加事件日志。

## 1.5.0 - 2026-09-11

- 工作区默认模板不再预设 `backend` 分类；分类作用域示例移至参考文档，已有工作区规则不受影响。
- 需求和设计结论收敛后直接生成草案；单一审阅对象接受简短肯定回复，批准后无新阻塞时同轮进入下一阶段。
- 执行循环使用精简上下文投影；工作台查询新增带来源的接手包与当前工作区历史文档检索。

## 1.4.0 - 2026-09-11

### 破坏性变更与升级动作

- `workspace.json` 主版本升级为 2；存量工作区需先完成迁移，迁移前 `status` 会给出引导并保留原状态备份。

- 修复任务凭据核对：需求 artifacts 下的交付物改为按工作区根定位，不再被误拼到目标仓路径下；工作台只读查询同步修复，需求与验证查询恢复可用。
- 精简根规范正文，恢复体检对根规范体积上限的通过状态，并新增守护测试防止再次超限。
- 离线检查清单统一改为按目录校验全部 Schema，新增 Schema 不再需要维护多处清单；移除无实际约束的初始化输入 Schema 别名文件。

## 1.3.0 - 2026-09-09

- 新增供本地工作台使用的只读查询，展示多仓工作区、全生命周期需求、完整计划与文档、验证证据、扩展流程和独立运行记录。
- 验证记录与当前代码适用性分开展示，并保留每个仓库的匹配、变化或无法核对原因；查看记录不会执行测试或推进工作流。
- 查询支持版本协商、分页、文档版本定位、超时和读取上限；单个记录损坏时保留其他可读记录并报告诊断。
- 原有 Agent 状态与续接入口保持兼容，不要求补充新的展示字段，也不迁移已有工作区。使用工作台插件时，需要将其绑定的工作流仓更新到本版本。
- 优化需求设计与实施计划的阅读结构、引用和续接说明，计划继续按完整审阅结果推进。

## 1.2.0 - 2026-09-07

- 标准需求新增书面设计和书面计划的人工审阅门禁；计划获批后才进入实现。
- 将需求设计、计划、执行和验证拆成四个按阶段加载的 Core Skill，并增加计划和执行上下文测量路径。
- 实施计划按独立可验证交付结果拆分，执行阶段包含任务级验证、根因调试、实际 diff 自审和整体复核。
- 验证记录升级为绑定当前 Git 代码状态的完整批次；旧执行记录保留可读，但不能推动提测或完成。

## 1.1.0 - 2026-09-07

- 标准需求改为逐阶段讨论和确认：需求、方案、实施计划分别确认后才生成对应记录。
- 新建标准需求只创建需求记录；设计、计划和验证记录按实际阶段出现，避免无效占位内容。
- 状态和续接摘要依据最新的实际验证记录判断，不再把验证文件存在视为验证通过。
- 标准 Workflow Run 可以在验证记录生成前启动；已有验证记录继续保留并追加 Action 状态。
- 使用者可通过更新流程直接获得新 Core 规则，无需迁移 `.workspace/`；会写入需求相关文件的本地 Extension 需要按更新提示审阅其 Skill。
- 新增面向维护者的架构说明，集中说明组件职责、工作流、状态与 Extension 机制。

## 1.0.0 - 2026-09-06

首个公开发布版本。

- 提供本地 Agent 多仓工作流、需求记录、离线验证与可选本地 Extension。
- `.workspace/` 保存使用者状态，不随公共 Kit 更新覆盖。
- Extension Action 需要明确的确认摘要；未知格式会被拒绝。
